# 基于SpringBoot的AI校园综合服务平台 —— 程序开发方案

> 对应开题报告三端架构：**Web学生前台 + Web管理后台（Web端）**、**微信小程序（小程序端）**，共用同一套 SpringBoot 后端。

---

## 一、总体架构

```
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│ Web学生前台  │  │ Web管理后台  │  │  微信小程序  │
│ Vue3+ElemPlus│  │ Vue3+ECharts│  │ 原生小程序   │
└──────┬──────┘  └──────┬──────┘  └──────┬──────┘
       └────────────────┼────────────────┘
                        ▼ HTTPS / RESTful JSON
              ┌───────────────────┐
              │  SpringBoot 后端   │
              │ Controller→Service│
              │ →Mapper(MP)       │
              │ ├ AI网关层(独立)   │
              │ ├ Redis(缓存/限流) │
              │ └ MinIO(文件存储)  │
              └─────────┬─────────┘
                ┌───────┴───────┐
                ▼               ▼
             MySQL 8      第三方大模型API
```

- 后端：`SpringBoot 3.x + MyBatis-Plus + MySQL 8 + Redis + MinIO + JWT + PDFBox`
- 三端通过统一 RESTful API 交互，JWT 统一鉴权；小程序通过 `wx.login` code 换取与 Web 端互通的账号体系。

---

## 二、Web端开发方案

Web端包含两个独立的 Vue3 工程：**学生前台** 和 **管理后台**。

### 2.1 技术选型（两端共用基础）

| 项 | 选型 |
|---|---|
| 框架 | Vue 3（Composition API + `<script setup>`） |
| 构建 | Vite |
| 语言 | JavaScript（毕设体量可不强制 TS） |
| UI 组件库 | Element Plus |
| 路由 / 状态 | Vue Router 4 / Pinia |
| HTTP | Axios（统一封装拦截器：token注入、401跳转、错误提示） |
| 富文本/Markdown | mavon-editor / wangEditor（动态、公告） |
| AI对话渲染 | markdown-it + highlight.js（代码纠错结果高亮） |
| 可视化（仅管理端） | ECharts 5 |

### 2.2 Web学生前台

**目录结构**

```
web/frontend/student/
├─ src/
│  ├─ api/            # 按模块拆分：ai.js idle.js activity.js lostfound.js user.js...
│  ├─ assets/
│  ├─ components/     # 通用组件：NavBar、ItemCard、CommentList、UploadImg...
│  ├─ layout/         # 主布局（顶栏+侧边+内容区）
│  ├─ router/         # 路由表 + 登录守卫
│  ├─ store/          # Pinia：user、message
│  ├─ utils/          # request.js封装、日期格式化、敏感字段脱敏
│  └─ views/
│     ├─ login/       # 登录注册（账号密码 + 小程序扫码绑定可选）
│     ├─ home/        # 首页聚合：公告轮播 + 各模块入口
│     ├─ ai/          # AI学习中心：答疑对话、代码纠错、PDF解析问答、提纲生成、错题本、智能习题
│     ├─ idle/        # 闲置互换：列表/发布/详情/预约/评价
│     ├─ activity/    # 活动组队：列表/发布/详情/报名/签到/我的队伍
│     ├─ lostfound/   # 失物招领：发布/检索/详情/认领
│     ├─ social/      # 动态广场：发布/点赞/评论
│     ├─ notice/      # 校园公告列表与详情
│     ├─ message/     # 消息通知中心
│     └─ profile/     # 个人中心：资料、我的发布、我的报名、错题本
```

**关键实现要点**

1. **AI学习中心（核心页面）**
   - 左侧会话列表 + 右侧对话窗，流式输出（SSE 或轮询分段返回）；
   - PDF解析问答：`<el-upload>` 上传PDF → 后端PDFBox提取 → 返回后可针对文档多轮提问；
   - 代码纠错：代码编辑器（CodeMirror/Monaco）+ 语言选择，结果用 markdown-it 渲染并高亮；
   - 错题本：错题列表按学科标签筛选，「生成同类习题」按钮调 AI 生成。
2. **图片上传**：统一封装上传组件 → 先传 MinIO 返回 URL，再随表单提交。
3. **登录态**：Pinia 持久化 token（localStorage），Axios 请求头统一携带；路由守卫拦截未登录访问。
4. **响应式**：按 PC 为主设计，≥1200px 布局。

### 2.3 Web管理后台

**目录结构**

```
web/frontend/admin/
├─ src/
│  ├─ api/            # user.js audit.js report.js aiConfig.js stats.js notice.js
│  ├─ layout/         # 经典后台布局：侧边菜单 + 顶栏 + tags-view
│  ├─ router/         # 动态路由（按角色权限生成菜单）
│  └─ views/
│     ├─ dashboard/   # 数据可视化大屏（ECharts）
│     ├─ user/        # 用户管理：列表/禁用/解封/重置密码
│     ├─ audit/       # 内容审核：闲置/活动/失物 待审队列，通过/驳回
│     ├─ report/      # 举报处理：接收/核查/处置闭环
│     ├─ notice/      # 公告发布管理
│     ├─ ai/          # AI配置：模型参数、提示词模板、限流规则、调用日志
│     └─ system/      # 管理员账号、角色权限
```

**关键实现要点**

1. **数据大屏（Dashboard）**：ECharts 实现
   - 折线图：近30天用户增长、AI调用量趋势；
   - 柱状图：各模块发布量（闲置/活动/失物）；
   - 饼图：失物招领状态占比、举报类型占比；
   - 数字卡片：总用户数、今日活跃、今日AI调用次数。
2. **审核工作流**：待审核列表（tab切换三类内容）→ 详情弹窗 → 通过/驳回（填驳回理由）→ 消息通知作者。
3. **举报闭环**：举报列表 → 关联内容查看 → 处置（下架内容/警告/封禁用户）→ 状态流转（待处理→已处理）。
4. **AI配置页**：表单化配置模型名称、temperature、最大token、提示词模板（可编辑）、每用户每日限流次数；调用日志表格可追溯。
5. **权限**：管理员登录独立接口 + 角色字段，路由按权限动态注册。

---

## 三、小程序端开发方案

### 3.1 技术选型

| 项 | 选型 |
|---|---|
| 框架 | 原生微信小程序（WXML/WXSS/JS，不引入uni-app，与开题一致） |
| 请求 | 封装 `utils/request.js`（Promise化 wx.request + token + 错误处理） |
| 状态 | 全局 `app.js` globalData + 本地 storage |
| UI | 自绘 + 可引 WeUI 组件库 |
| 分包 | 主包（首页/我的/登录）+ 子包（AI学习/闲置/活动/失物） |

### 3.2 页面结构

```
miniprogram/
├─ app.js / app.json / app.wxss
├─ pages/
│  ├─ index/          # 首页：公告轮播 + 功能宫格（8个入口）
│  ├─ login/          # 微信一键登录（wx.login）+ 账号绑定
│  ├─ notice/         # 公告列表/详情
│  ├─ message/        # 消息通知
│  └─ my/             # 我的：资料、我的发布、我的报名、错题本、设置
├─ pages-ai/          # 子包：AI答疑对话、代码纠错、PDF问答（跳web-view可选）、提纲、错题
├─ pages-idle/        # 子包：闲置列表/发布/详情/预约/评价
├─ pages-activity/    # 子包：活动列表/发布/详情/报名/签到（wx.scanCode扫码签到）
└─ pages-lostfound/   # 子包：失物招领列表/发布/详情
```

### 3.3 关键实现要点

1. **统一登录机制（与Web互通）**
   - 小程序：`wx.login()` 拿 code → 后端 `code2session` 换 openid → 查用户表，未注册则自动创建账号 → 返回与 Web 端同一套 JWT；
   - 支持在「我的」中绑定学号/手机号，实现 Web 端账号密码登录与小程序登录指向同一用户。
2. **AI对话页**：对话气泡列表 + 上拉加载历史；由于小程序 SSE 支持受限，采用一次性返回（loading 动画）或 `wx.request` 分片轮询；代码块用 rich-text/towxml 渲染。
3. **PDF解析**：`wx.chooseMessageFile` 从聊天选择 PDF 上传 → 后端解析后进入问答页。
4. **活动签到**：发布者展示签到二维码（后端生成）→ 参与者 `wx.scanCode` 完成签到。
5. **图片上传**：`wx.chooseMedia` + `wx.uploadFile` 直传后端 → MinIO。
6. **消息触达**：服务通知用微信小程序订阅消息（报名成功、审核结果、预约提醒）。
7. **多端一致性**：与 Web 前台共用同一套 API，字段、交互流程保持一致，仅按移动端重排 UI。

---

## 四、后端开发方案（三端共用，简述）

1. **模块划分**（单工程多包即可，毕设不必微服务）：
   `controller / service / mapper / entity / dto / config / interceptor / aigateway / utils`
2. **AI网关层**：独立 `AiGatewayService`，统一封装大模型API调用——上下文记忆（会话表存历史消息，截断窗口）、提示词模板（DB可配）、Redis限流（每用户每日次数+令牌桶）、超时重试、敏感词过滤（DFA）。
3. **鉴权**：JWT + 拦截器；学生端/管理端角色字段区分；小程序 openid 与 user 表绑定。
4. **数据库核心表**（MySQL）：
   `user`、`admin`、`ai_session`、`ai_message`、`wrong_question`、`idle_item`、`idle_appointment`、`idle_comment`、`activity`、`activity_member`、`activity_signin`、`lost_found`、`notice`、`post`、`post_comment`、`report`、`ai_call_log`、`ai_config`
5. **文件存储**：MinIO 统一存图片/PDF，接口返回可访问URL。
6. **测试**：Postman 接口测试 → 功能测试 → 多端兼容性测试 → AI稳定性压测（JMeter 简单并发）。

---

## 五、开发顺序建议（迭代式）

| 阶段 | 内容 | 产出 |
|---|---|---|
| 1. 地基 | 数据库建表、后端工程搭建、登录鉴权（Web+小程序）、MinIO/Redis 接入 | 可登录的三端骨架 |
| 2. AI核心 | AI网关、答疑对话、提示词模板、PDF解析、代码纠错 | AI学习中心可用 |
| 3. 生活模块 | 闲置互换 → 活动组队 → 失物招领 → 公告/动态/消息 | 学生端功能完整 |
| 4. 管理后台 | 用户管理、审核、举报闭环、AI配置 | 管控闭环 |
| 5. 数据大屏 | 统计接口 + ECharts 大屏 | Dashboard |
| 6. 收尾 | 测试、修bug、性能优化、部署（Nginx + 后端 jar + 小程序提审） | 可答辩版本 |

> 建议先做「登录 + AI答疑 + 闲置互换」这条最短闭环跑通三端联调，再并行扩展其余模块，答辩演示链路最稳。
