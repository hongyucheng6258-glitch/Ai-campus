# AI校园综合服务平台 - 部署说明

## 一、环境要求

| 组件 | 版本要求 |
|------|----------|
| JDK | 17 或更高 |
| Maven | 3.8 或更高 |
| Node.js | 18 或更高 |
| npm | 随 Node.js 安装 |
| MySQL | 8.0 |
| Redis | 6.0 或更高 |
| MinIO | 可选（图片已改为数据库存储，MinIO 仅用于兼容旧接口） |
| 微信开发者工具 | 最新稳定版（小程序开发时需要） |

## 二、数据库部署

### 2.1 创建数据库

```sql
CREATE DATABASE ai_campus_platform DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
```

### 2.2 初始化表结构

执行 `web/backend/src/main/resources/db/schema.sql`：

```bash
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/schema.sql
```

### 2.3 导入测试数据（可选）

```bash
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/reset_and_testdata.sql
```

### 2.4 执行迁移脚本（如从旧版本升级）

按版本顺序执行：

```bash
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/migrate_v5_images_to_db.sql
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/migrate_v6_all_uploads_to_db.sql
```

### 2.5 MySQL 配置建议

由于图片采用 Base64 入库，MySQL 需要调大 `max_allowed_packet`：

```ini
[mysqld]
max_allowed_packet=128M
```

## 三、Redis 部署

### 3.1 安装并启动

Windows 环境可下载 Redis for Windows，或使用 WSL 运行原生 Redis。

```bash
redis-server
```

默认端口 6379，无密码。生产环境建议设置密码。

### 3.2 验证

```bash
redis-cli ping
# 返回 PONG 即正常
```

## 四、后端部署

### 4.1 配置环境变量

以下环境变量覆盖 `application.yml` 中的默认值，生产环境务必修改：

| 环境变量 | 说明 | 默认值 |
|----------|------|--------|
| `MYSQL_HOST` | MySQL 主机 | localhost |
| `MYSQL_PORT` | MySQL 端口 | 3306 |
| `MYSQL_DB` | 数据库名 | ai_campus_platform |
| `MYSQL_USER` | 数据库用户 | root |
| `MYSQL_PASSWORD` | 数据库密码 | 666666 |
| `REDIS_HOST` | Redis 主机 | localhost |
| `REDIS_PORT` | Redis 端口 | 6379 |
| `REDIS_PASSWORD` | Redis 密码 | （空） |
| `MINIO_ENDPOINT` | MinIO 地址 | http://localhost:9000 |
| `MINIO_ACCESS_KEY` | MinIO 账号 | admin |
| `MINIO_SECRET_KEY` | MinIO 密钥 | 12345678 |
| `AI_BASE_URL` | AI 服务地址 | https://api.deepseek.com |
| `AI_API_KEY` | AI 服务密钥 | sk-xxx（占位符） |
| `AI_MODEL` | AI 模型名 | deepseek-chat |
| `WX_APPID` | 微信小程序 AppID | wx-placeholder-appid |
| `WX_SECRET` | 微信小程序 Secret | wx-placeholder-secret |
| `JWT_SECRET` | JWT 签名密钥 | campus-platform-jwt-secret-key-... |
| `SIGNIN_SECRET` | 活动签到 HMAC 密钥 | campus-activity-signin-hmac-secret |
| `TRUSTED_ORIGINS` | 可信来源（逗号分隔） | http://localhost:5173,... |

### 4.2 构建与启动

```bash
cd web/backend
mvn clean package -DskipTests
java -jar target/platform-server.jar
```

开发环境可直接运行：

```bash
cd web/backend
mvn spring-boot:run
```

后端默认端口 8080，接口统一前缀 `/api`。

### 4.3 AI 配置

AI 密钥有两种配置方式：

1. **环境变量**：设置 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`
2. **数据库**：在 `ai_config` 表中配置对应 key，管理后台可在线修改，优先级高于环境变量

### 4.4 验证

```bash
curl http://localhost:8080/api/announcements
```

返回 JSON 数据即正常。

## 五、学生端部署

### 5.1 安装依赖

```bash
cd web/frontend/student
npm install
```

### 5.2 开发模式

```bash
npm run dev
```

开发服务器默认端口 5173，已配置代理将 `/api` 和 `/ws` 转发到后端 8080。

### 5.3 生产构建

```bash
npm run build
```

构建产物在 `dist/` 目录，可使用 Nginx 或其他 Web 服务器托管。

### 5.4 Nginx 参考配置

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        root /path/to/dist;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /ws {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

## 六、管理端部署

### 6.1 安装依赖

```bash
cd web/frontend/admin
npm install
```

### 6.2 开发模式

```bash
npm run dev
```

开发服务器默认端口 5174，已配置代理将 `/api` 转发到后端 8080。

### 6.3 生产构建

```bash
npm run build
```

构建产物在 `dist/` 目录，Nginx 配置与学生端类似，无需 WebSocket 代理。

## 七、小程序部署

### 7.1 导入项目

1. 打开微信开发者工具
2. 选择「导入项目」，目录指向 `miniprogram/frontend`
3. AppID 使用 `project.config.json` 中的配置，或替换为自己的 AppID

### 7.2 配置后端地址

在小程序的请求工具（`miniprogram/frontend/utils/request.js`）中修改后端接口地址，指向已部署的后端服务。

### 7.3 预览与上传

1. 点击「编译」预览小程序
2. 点击「预览」生成二维码在手机上体验
3. 点击「上传」提交代码到微信后台审核发布

### 7.4 微信登录配置

后端 `application.yml` 中的 `wx.miniapp.appid` 和 `wx.miniapp.secret` 需替换为小程序的实际 AppID 和 Secret，或通过环境变量 `WX_APPID`、`WX_SECRET` 设置。

## 八、测试

### 8.1 后端测试

```bash
cd web/backend
mvn test
```

### 8.2 学生端测试

```bash
cd web/frontend/student
npm test
```

### 8.3 管理端测试

```bash
cd web/frontend/admin
npm test
```

### 8.4 小程序测试

```bash
cd miniprogram/frontend
npm test
```

## 九、常见问题

### 9.1 后端启动报数据库连接失败

检查 MySQL 是否启动、端口是否正确、账号密码是否匹配。确认 `max_allowed_packet` 已调大至 128MB。

### 9.2 Redis 连接失败

确认 Redis 服务已启动，端口 6379 未被占用。如设置了密码，需配置 `REDIS_PASSWORD` 环境变量。

### 9.3 AI 功能不可用

检查 `AI_API_KEY` 环境变量是否设置，或在管理后台「AI 配置」页面填入有效的 API Key。

### 9.4 图片上传失败

图片已改为 Base64 存储到数据库。确认 MySQL `max_allowed_packet` 足够大（建议 128MB），`max-request-size` 已调整为 64MB。

### 9.5 WebSocket 连接失败

学生端聊天功能依赖 WebSocket。开发环境 Vite 已配置 `/ws` 代理；生产环境需在 Nginx 中配置 WebSocket 代理（见 5.4）。

### 9.6 小程序登录失败

确认后端 `WX_APPID` 和 `WX_SECRET` 与小程序后台一致。微信开发者工具中需关闭「不校验合法域名」选项。

### 9.7 跨域问题

后端通过 `TRUSTED_ORIGINS` 环境变量配置可信来源白名单，生产环境需将前端域名加入白名单。

### 9.8 MinIO 上传报 SignatureDoesNotMatch

`minio.access-key` 和 `minio.secret-key` 必须与启动 MinIO 时的 `MINIO_ROOT_USER` 和 `MINIO_ROOT_PASSWORD` 一致。
